# AI Prompt Firewall — pinned dependencies
#
# Versions are pinned exactly so the environment can be rebuilt as it was,
# rather than resolving to whatever happens to be current months later.
# Nothing here requires a GPU, downloads a model, or contacts a remote
# inference service: the service runs entirely on CPU.
#
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt

# --- Service ---
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5

# --- Testing ---
# httpx is required by fastapi.testclient.
pytest==9.1.1
httpx==0.28.1

# --- Dataset construction (build-time only) ---
# Used for the distilbert-base-uncased tokenizer alone, to verify the corpus
# against the 512-token ceiling. It pulls no torch and needs no GPU, and no
# module under app/ imports it -- the service never loads it at runtime.
# The vocabulary is fetched from the model hub the first time it runs.
transformers==5.17.0

# --- Phase 5 (Stage 2 inference) ---
# transformers and torch are NOT listed yet. They arrive when the fine-tuned
# model artifact does, and torch must be installed from the CPU-only wheel
# index to avoid pulling a multi-gigabyte CUDA build onto a laptop.
